# -*- mode: snippet -*-
# name: ksvc
# key: ksvc
# expand-env: ((yas/indent-line 'fixed))
# --
apiVersion: v1
kind: Service
metadata:
  name: $1
  labels:
    $2: $3
spec:
  selector:
    $2: $3
  type: ClusterIP
  ports:
  - name: $4
    port: $5
    targetPort: $5